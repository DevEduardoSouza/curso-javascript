// Adicione o c�digo JavaScript aqui

/** ARRAYS
 * 
 *  - São objetos, porém serve como lista
 *  - Podemos ter vários tipos de dados
 *  - por índice
 */

var vet = [1, "Eduardo", {teste: 1}];
console.log(vet);

console.log(vet[1]);

// Inserir um elemento, sem métodos
vet[4] = 10;
console.log(vet[4]);

// Sobrescrever
vet[0] = 2;
console.log(vet[0]);